    public OpenAction(Container container, XSLTReader reader, XSLTWriter writer, MainMenuBar menu) {
        super("Open...", null);
        this.container = container;
        this.reader = reader;
        this.writer = writer;
        this.menu = menu;
    }
