package cz.langteacher.model;

/**
 * Settings of LangTeacher client, it is persisted to DB
 * @author libor
 *
 */
public final class Setting {

    private String attribute;

    private String value;

    public Setting() {
        super();
    }

    public Setting(String attribute, String value) {
        super();
        this.attribute = attribute;
        this.value = value;
    }

    public String getAttribute() {
        return attribute;
    }

    public String getValue() {
        return value;
    }

    /**
	 * @generated by CodeSugar http://sourceforge.net/projects/codesugar */
    public String toString() {
        StringBuffer buffer = new StringBuffer();
        buffer.append("[Setting:");
        buffer.append(" attribute: ");
        buffer.append(attribute);
        buffer.append(" value: ");
        buffer.append(value);
        buffer.append("]");
        return buffer.toString();
    }

    /**
	 * Override hashCode.
	 *
	 * @return the Objects hashcode.
	 */
    public int hashCode() {
        int hashCode = 1;
        hashCode = 31 * hashCode + (attribute == null ? 0 : attribute.hashCode());
        hashCode = 31 * hashCode + (value == null ? 0 : value.hashCode());
        return hashCode;
    }

    /**
	 * Returns <code>true</code> if this <code>Setting</code> is the same as the o argument.
	 *
	 * @return <code>true</code> if this <code>Setting</code> is the same as the o argument.
	 */
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null) {
            return false;
        }
        if (o.getClass() != getClass()) {
            return false;
        }
        Setting castedObj = (Setting) o;
        return ((this.attribute == null ? castedObj.attribute == null : this.attribute.equals(castedObj.attribute)) && (this.value == null ? castedObj.value == null : this.value.equals(castedObj.value)));
    }

    public void setAttribute(String attribute) {
        this.attribute = attribute;
    }

    public void setValue(String value) {
        this.value = value;
    }
}
