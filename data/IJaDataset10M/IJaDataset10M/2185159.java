package de.sljngshot.util;

public class UpdateManager {

    /**
	 * This method triggers update checking. It supports two modes: Intrusive
	 * (where the user gets notified when the update checking did not succeed)
	 * and non-intrusive (where the user only gets notified that a new version
	 * is available)
	 * 
	 * @param intrusive
	 *            whether to use intrusive mode or not
	 */
    public static void checkForUpdates(boolean intrusive) {
    }
}
