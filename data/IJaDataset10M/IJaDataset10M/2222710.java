package csa.jportal.match.display;

import java.awt.*;
import javax.swing.*;
import java.io.*;
import csa.gui.*;

/**
 *
 * @author Malban
 */
public class BackdropPanel extends javax.swing.JPanel {

    /** Creates new form PausePanel */
    JLabel mLabel;

    int darkness = 200;

    Color backGround = new java.awt.Color(0, 0, 0, darkness);

    public void setDarkness(int d) {
        darkness = d;
        backGround = new java.awt.Color(0, 0, 0, darkness);
    }

    public BackdropPanel() {
        initComponents();
        setOpaque(false);
        setDoubleBuffered(false);
    }

    public void setLabel(JLabel label) {
        removeAll();
        add(label, BorderLayout.CENTER);
        mLabel = label;
        setComponentZOrder(label, 0);
    }

    public void deinit() {
        setVisible(false);
        removeAll();
        mLabel = null;
    }

    @Override
    public void paintComponent(java.awt.Graphics g) {
        g.setColor(backGround);
        g.fillRect(0, 0, getWidth(), getHeight());
        super.paintComponent(g);
    }

    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    private void initComponents() {
        setDoubleBuffered(false);
        setLayout(new java.awt.BorderLayout());
    }
}
