package org.middleheaven.quantity.time;

import org.middleheaven.util.Incrementor;

public class TimePointIncrementor implements Incrementor<TimePoint> {

    @Override
    public TimePoint increment(TimePoint object) {
        return null;
    }
}
