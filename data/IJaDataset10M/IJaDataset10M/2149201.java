package jasperdesign.ui;

import net.sf.jasperreports.engine.*;
import net.sf.jasperreports.engine.print.*;
import java.io.*;
import java.awt.BorderLayout;
import net.sf.jasperreports.engine.print.*;
import net.sf.jasperreports.view.*;
import java.awt.*;
import java.awt.event.*;

/**
 *
 */
public class ReportViewer extends javax.swing.JFrame {

    /** Creates new form ReportViewer */
    public ReportViewer(JasperPrint jasperPrint) throws JRException {
        initComponents();
        Viewer viewer = new Viewer(jasperPrint);
        getContentPane().add(viewer, BorderLayout.CENTER);
        pack();
        setExtendedState(MAXIMIZED_BOTH);
        setSize(Toolkit.getDefaultToolkit().getScreenSize());
    }

    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    private void initComponents() {
        setTitle("ReportViewer");
        addWindowListener(new java.awt.event.WindowAdapter() {

            public void windowClosing(java.awt.event.WindowEvent evt) {
                exitForm(evt);
            }
        });
        getContentPane().setLayout(new java.awt.BorderLayout());
        pack();
    }

    /** Exit the Application */
    private void exitForm(java.awt.event.WindowEvent evt) {
        setVisible(false);
        dispose();
    }

    class Viewer extends JRViewer {

        Viewer(JasperPrint p) throws JRException {
            super(p);
            cmbZoom.setSelectedIndex(5);
        }
    }
}
