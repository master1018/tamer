package com.jpcomponents;

import java.awt.Graphics;
import java.awt.Image;
import javax.swing.JComponent;
import javax.swing.JPanel;

/**
 *
 * @author Pablo
 */
public class JPBackgroundPanel extends JComponent {

    private Image image;

    /** Si se debe o no estirar la imagen para que ocupe todo el panel */
    private boolean stretch = true;

    /** Posicion x para la imagen */
    private int xLocation = 0;

    /** Posición y para la imagen */
    private int yLocation = 0;

    /** Creates new form JPBackgroundPanel */
    public JPBackgroundPanel() {
        initComponents();
        this.setOpaque(true);
    }

    public JPBackgroundPanel(Image image) {
        this.image = image;
        initComponents();
        this.setOpaque(true);
    }

    public Image getImage() {
        return image;
    }

    public void setImage(Image image) {
        this.image = image;
    }

    public boolean isStretch() {
        return stretch;
    }

    public void setStretch(boolean stretch) {
        this.stretch = stretch;
    }

    public int getxLocation() {
        return xLocation;
    }

    public void setxLocation(int xLocation) {
        this.xLocation = xLocation;
    }

    public int getyLocation() {
        return yLocation;
    }

    public void setyLocation(int yLocation) {
        this.yLocation = yLocation;
    }

    @Override
    protected void paintComponent(Graphics g) {
        if (this.image != null) {
            if (stretch) {
                g.drawImage(image, xLocation, yLocation, getWidth(), getHeight(), this);
            } else {
                g.drawImage(image, xLocation, yLocation, this);
            }
        }
        super.paintComponent(g);
    }

    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    private void initComponents() {
        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(this);
        this.setLayout(layout);
        layout.setHorizontalGroup(layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING).addGap(0, 400, Short.MAX_VALUE));
        layout.setVerticalGroup(layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING).addGap(0, 300, Short.MAX_VALUE));
    }
}
