package netxrv.jnlp.tardiff;

public interface TarConstants {

    int TAR_BLOCK_SIZE = 512;

    int FILE_NAME_SIZE = 100;

    int FILE_MODE_SIZE = 8;

    int OWNER_USER_ID_SIZE = 8;

    int GROUP_USER_ID_SIZE = 8;

    int FILE_SIZE_IN_BYTES_SIZE = 12;

    int LAST_MODIFICATION_TIME_SIZE = 12;

    int CHECK_SUM_FOR_HEADER_BLOCK_SIZE = 8;

    int LINK_INDICATOR_SIZE = 1;

    int NAME_OF_LINKED_FILE_SIZE = 100;

    int FILE_NAME_OFFSET = 0;

    int FILE_MODE_OFFSET = FILE_NAME_OFFSET + FILE_NAME_SIZE;

    int OWNER_USER_ID_OFFSET = FILE_MODE_OFFSET + FILE_MODE_SIZE;

    int GROUP_USER_ID_OFFSET = OWNER_USER_ID_OFFSET + OWNER_USER_ID_SIZE;

    int FILE_SIZE_IN_BYTES_OFFSET = GROUP_USER_ID_OFFSET + GROUP_USER_ID_SIZE;

    int LAST_MODIFICATION_TIME_OFFSET = FILE_SIZE_IN_BYTES_OFFSET + FILE_SIZE_IN_BYTES_SIZE;

    int CHECK_SUM_FOR_HEADER_BLOCK_OFFSET = LAST_MODIFICATION_TIME_OFFSET + LAST_MODIFICATION_TIME_SIZE;

    int LINK_INDICATOR_OFFSET = CHECK_SUM_FOR_HEADER_BLOCK_OFFSET + CHECK_SUM_FOR_HEADER_BLOCK_SIZE;

    int NAME_OF_LINKED_FILE_OFFSET = LINK_INDICATOR_OFFSET + LINK_INDICATOR_SIZE;
}
