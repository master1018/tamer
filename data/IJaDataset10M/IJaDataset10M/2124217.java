package org.dbgen;

import java.util.*;
import org.dbgen.support.*;

/**
 * This class was generated by a SmartGuide.
 *
 */
public class Util {

    /**
   * This method was created by a SmartGuide.
   * @return java.lang.String[]
   * @param vector Vector
   */
    public static String[] convertVectorToStringArray(Vector vector) {
        String[] result = new String[vector.size()];
        for (int i = 0; i < vector.size(); i++) {
            result[i] = (String) vector.elementAt(i);
        }
        return result;
    }

    /**
   * This method was created by a SmartGuide.
   * @return java.lang.String
   */
    public static String getDateString() {
        java.util.GregorianCalendar cal = new java.util.GregorianCalendar();
        java.text.SimpleDateFormat sdf = new java.text.SimpleDateFormat();
        return sdf.format(cal.getTime());
    }

    /**
   * This method was created by a SmartGuide.
   * @return boolean
   * @param name java.lang.String
   */
    public static boolean isValidClassName(String name) {
        if (name == null || StringHelper.isBlank(name)) return false;
        if (!StringHelper.isAlpha(name.charAt(0))) return false;
        for (int i = 1; i < name.length(); i++) {
            char c = name.charAt(i);
            if (!StringHelper.isAlpha(c) && !StringHelper.isNumeric(c) && (c != '_')) return false;
        }
        return true;
    }
}
