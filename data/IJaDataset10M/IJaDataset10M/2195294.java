package jshm.sh;

import java.text.SimpleDateFormat;

public class DateFormats {

    public static final SimpleDateFormat GH_MANAGE_SCORES = new SimpleDateFormat("MMM. d, yyyy, h:mma"), GH_VIEW_SCORES = new SimpleDateFormat("MMM. d, yyyy @ h:mm a");
}
