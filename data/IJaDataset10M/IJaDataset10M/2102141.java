package modelo;

/**
 * Locatario generated by hbm2java
 */
public class Locatario implements java.io.Serializable {

    private Integer codigoLocatario;

    private Integer matricula;

    private String nome;

    private Boolean ativo;

    private String urlFoto;

    @Override
    public boolean equals(Object obj) {
        if (obj == null) {
            return false;
        }
        if (getClass() != obj.getClass()) {
            return false;
        }
        final Locatario other = (Locatario) obj;
        if (this.codigoLocatario != other.codigoLocatario && (this.codigoLocatario == null || !this.codigoLocatario.equals(other.codigoLocatario))) {
            return false;
        }
        if (this.matricula != other.matricula && (this.matricula == null || !this.matricula.equals(other.matricula))) {
            return false;
        }
        if ((this.nome == null) ? (other.nome != null) : !this.nome.equals(other.nome)) {
            return false;
        }
        if (this.ativo != other.ativo && (this.ativo == null || !this.ativo.equals(other.ativo))) {
            return false;
        }
        if ((this.urlFoto == null) ? (other.urlFoto != null) : !this.urlFoto.equals(other.urlFoto)) {
            return false;
        }
        if ((this.observacao == null) ? (other.observacao != null) : !this.observacao.equals(other.observacao)) {
            return false;
        }
        return true;
    }

    @Override
    public int hashCode() {
        int hash = 3;
        hash = 19 * hash + (this.codigoLocatario != null ? this.codigoLocatario.hashCode() : 0);
        hash = 19 * hash + (this.matricula != null ? this.matricula.hashCode() : 0);
        hash = 19 * hash + (this.nome != null ? this.nome.hashCode() : 0);
        hash = 19 * hash + (this.ativo != null ? this.ativo.hashCode() : 0);
        hash = 19 * hash + (this.urlFoto != null ? this.urlFoto.hashCode() : 0);
        hash = 19 * hash + (this.observacao != null ? this.observacao.hashCode() : 0);
        return hash;
    }

    private String observacao;

    public Locatario() {
    }

    public Locatario(Integer matricula, String nome, Boolean ativo, String urlFoto, String observacao) {
        this.matricula = matricula;
        this.nome = nome;
        this.ativo = ativo;
        this.urlFoto = urlFoto;
        this.observacao = observacao;
    }

    public Integer getCodigoLocatario() {
        return this.codigoLocatario;
    }

    public void setCodigoLocatario(Integer codigoLocatario) {
        this.codigoLocatario = codigoLocatario;
    }

    public Integer getMatricula() {
        return this.matricula;
    }

    public void setMatricula(Integer matricula) {
        this.matricula = matricula;
    }

    public String getNome() {
        return this.nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public Boolean getAtivo() {
        return this.ativo;
    }

    public void setAtivo(Boolean ativo) {
        this.ativo = ativo;
    }

    public String getUrlFoto() {
        return this.urlFoto;
    }

    public void setUrlFoto(String urlFoto) {
        this.urlFoto = urlFoto;
    }

    /**
     * @return the observacao
     */
    public String getObservacao() {
        return observacao;
    }

    /**
     * @param observacao the observacao to set
     */
    public void setObservacao(String observacao) {
        this.observacao = observacao;
    }
}
