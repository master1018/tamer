package rpr2_d18.fixedRecordRepresentations;

import java.util.Iterator;
import java.util.ArrayList;
import hla.rti1516e.encoding.DataElement;
import hla.rti1516e.encoding.ByteWrapper;
import hla.rti1516e.exceptions.RTIinternalError;
import hla.rti1516e.encoding.DecoderException;
import hla.rti1516e.encoding.HLAfixedRecord;
import rpr2_d18.enumeratedDataRepresentations.MinefieldFusingEnum32;
import rpr2_d18.enumeratedDataRepresentations.OMT13boolean;

/**
 * JAVADOC_COMMENTS
 * semantics : -NULL-
 *
 * @author : auto generated by DRDC FOM Generator
 */
public class MineFusingStruct implements HLAfixedRecord {

    private MinefieldFusingEnum32 Primary;

    private MinefieldFusingEnum32 Secondary;

    private OMT13boolean AntiHandlingDevice;

    /**
   * Constructor
   */
    public MineFusingStruct() throws RTIinternalError {
        Primary = new MinefieldFusingEnum32();
        Secondary = new MinefieldFusingEnum32();
        AntiHandlingDevice = new OMT13boolean();
    }

    /**
   * Encodes this element into the specified ByteWrapper.
   * 
   * @param byteWrapper
   */
    public void encode(ByteWrapper byteWrapper) {
        Primary.encode(byteWrapper);
        Secondary.encode(byteWrapper);
        AntiHandlingDevice.encode(byteWrapper);
    }

    /**
   * Decodes this element from the ByteWrapper
   * 
   * @param byteWrapper
   */
    public void decode(ByteWrapper byteWrapper) throws DecoderException {
        Primary.decode(byteWrapper);
        Secondary.decode(byteWrapper);
        AntiHandlingDevice.decode(byteWrapper);
    }

    /**
   * Decodes this element from the byte array
   * 
   * @param byte array
   */
    public void decode(byte[] bytes) throws DecoderException {
        Primary.decode(bytes);
        Secondary.decode(bytes);
        AntiHandlingDevice.decode(bytes);
    }

    /**
   * Returns the octet boundary of this element.
   * - not really relavent for data structures
   * - return the boundary of the first field
   * 
   * @return
   */
    public int getOctetBoundary() {
        return getEncodedLength() % 4;
    }

    /**
   * convert this data structure into a byte array
   * 
   */
    public byte[] toByteArray() {
        int size = getEncodedLength();
        byte[] result = new byte[size];
        byte[] temp;
        int pos = 0;
        temp = Primary.toByteArray();
        System.arraycopy(temp, 0, result, pos, Primary.getEncodedLength());
        temp = Secondary.toByteArray();
        System.arraycopy(temp, 0, result, pos, Secondary.getEncodedLength());
        temp = AntiHandlingDevice.toByteArray();
        System.arraycopy(temp, 0, result, pos, AntiHandlingDevice.getEncodedLength());
        return result;
    }

    /**
   * add an element to this data structure
   * - this should do nothing since this is a FIXED data structure
   * 
   * @param DataElement dataElement
   */
    public void add(DataElement dataElement) {
    }

    /**
   * get the full size of this data structure
   * - add up the size of each member field
   * 
   */
    public int size() {
        return getEncodedLength();
    }

    /**
   * get an element from this data structure
   * - this should do nothing since this is a FIXED data structure
   * 
   * @param int index
   */
    public DataElement get(int index) {
        return null;
    }

    /**
   * get an iterator for this data structure
   * - this should do nothing since this is a FIXED data structure
   * 
   * @param int index
   */
    public Iterator<DataElement> iterator() {
        return null;
    }

    /**
   * Returns the size in bytes of this element's encoding.
   * 
   * @return size
   */
    public int getEncodedLength() {
        int size = 0;
        size += Primary.getEncodedLength();
        size += Secondary.getEncodedLength();
        size += AntiHandlingDevice.getEncodedLength();
        return size;
    }

    public MinefieldFusingEnum32 getPrimary() {
        return this.Primary;
    }

    public MinefieldFusingEnum32 getSecondary() {
        return this.Secondary;
    }

    public OMT13boolean getAntiHandlingDevice() {
        return this.AntiHandlingDevice;
    }

    public void setPrimary(MinefieldFusingEnum32 Primary) {
        this.Primary = Primary;
    }

    public void setSecondary(MinefieldFusingEnum32 Secondary) {
        this.Secondary = Secondary;
    }

    public void setAntiHandlingDevice(OMT13boolean AntiHandlingDevice) {
        this.AntiHandlingDevice = AntiHandlingDevice;
    }
}
