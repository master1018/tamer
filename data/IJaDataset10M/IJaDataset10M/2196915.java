package org.docflower.page.pagecontrols.containers;

import org.docflower.model.DataModel;
import org.docflower.page.pagecontrols.IPageControl;
import org.eclipse.swt.widgets.*;
import org.w3c.dom.Node;

/**
 * This control generate the controls according to the element hierarchy 
 * found under the bound node.
 * 
 *
 */
public class AutoGeneratedPanel implements IPageControl {

    private Composite container;

    @Override
    public Control getControl() {
        return container;
    }

    @Override
    public void setup(Node node, DataModel dataModel) {
    }
}
