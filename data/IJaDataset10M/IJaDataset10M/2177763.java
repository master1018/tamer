package org.upfrost.mapping.classes;

import java.util.Date;
import java.util.List;
import org.upfrost.AbstractDAO;
import org.upfrost.MapperSession;

public class TestClass2DAO extends AbstractDAO<TestClass2> {

    public static String CREATE_HSQL = "CREATE TABLE test2 (id INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY, \"value\" VARCHAR(128), \"when\" DATETIME)";

    public static String DROP = "DROP TABLE test2";

    public long m_cacheExpirationDelayMillis = 1000 * 30;

    public TestClass2DAO(MapperSession session) {
        super(session);
    }

    @Override
    public long getCacheExpirationDelayMillis() {
        return m_cacheExpirationDelayMillis;
    }

    public void setCacheExpirationDelayMillis(long cacheExpirationDelayMillis) {
        m_cacheExpirationDelayMillis = cacheExpirationDelayMillis;
    }
}
