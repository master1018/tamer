package net.sf.buildbox.bbx;

/**
 * Filters standard input to standard output, applying selected coloring rules.
 * Alternatively, it can be used as a launcher, which allows to also handle stderr
 * Features:
 * - special rule sets for various tools (ant, maven, striclogging, javac/javadoc)
 * - generic rule set
 * - destinations: stdout (ansi terminal only == no windows), html, swing console 
 */
public class Colorizer {

    public static void main(String[] args) {
    }
}
