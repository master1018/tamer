package com.habitsoft.kiyaa.views;

public interface Focusable {

    /**
	 * Set focus to the first field in the view, in an unobtrusive way, so the user can immediately start
	 * using the view without clicking on it.
	 */
    public void focus();
}
